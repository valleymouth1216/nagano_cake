<h1>商品詳細</h1>

<table>
    <tr>
        <td><%= attachment_image_tag @item, :image, format: 'jpeg', size:"300x200" %></td>
        <td>&nbsp;</td>
        <td>
          <p>商品名 :<%= @item.name  %></p>
          <p>商品説明 :<%= @item.introduction %> <br></br></p>
          <p>ジャンル :<%= @item.genre.name  %></p>
          <p>税込価格 :<%= @item.price*1.1  %>(<%= @item.price  %>)円<br>(税抜価格)</br></p>
          <p>販売ステータス :
          <% if @item.is_active==true %>
            販売中
          <% else %>
            販売停止中
          <% end %>
          </p>
        </td>
    </tr>
</table>

<%= link_to "編集する",edit_admin_item_path(@item.id),class:"btn btn-sm btn-success" %>
