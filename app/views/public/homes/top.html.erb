<div class='container'>


  <div class="row mt-5">

    <div class='mb-5 col-md-2' style="border:2px solid silver">
      <table>
        <thead>
          <tr><th>ジャンル検索</th></tr>
        </thead>
        <tbody>
        <% @genres.each do |genre| %>
          <tr>
            <td>
              <% if genre.items.present? %>
              <%= link_to genre.name, items_path(genre_id: genre.id), class: "text-secondary" %>
              <% end %>
            </td>
          </tr>
        <% end %>
          </tbody>
      </table>
    </div>


   <div class='col-md-10'>
     <div class="image-area">
     <div class="text-area text-left font-weight-bold">
       <p class="text-top">ようこそ、ながのCAKEへ!</br>このサイトは、ケーキ販売のECサイトになります。</br>会員でない方も商品の閲覧はできますが、</br>購入には会員の登録が必要になります。</p>
       </div>
     </div>

      <h1 class="mb-3 mt-3"><span class="col-md-3 bg-light s-py-7 x-py-7" >新着商品</span></h1>
        <div class="row">
        <% @items.each do |item| %>
          <div class="card col-md-3">
            <p><%= link_to item_path(item.id )do %><%= attachment_image_tag item, :image, format: 'jpeg', width:"100%", height:"100%" %><% end %></p>
            <p><%= item.name %></br><%= number_to_currency(item.price, format: "%u%n", unit: "￥") %></p>
          </div>
        <% end %>
        </div>
    </div>

    </div>


      <div class="text-right">
        <%= link_to "全ての商品を見る＞", items_path %>
      </div>



</div>